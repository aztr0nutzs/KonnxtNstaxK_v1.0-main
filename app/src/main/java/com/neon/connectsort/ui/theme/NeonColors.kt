package com.neon.connectsort.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * A central object to hold the neon color palette for the app.
 * This ensures consistency and a single source of truth for all colors.
 */
object NeonColors {

    // Primary Neon Palette
    val neonCyan = Color(0xFF00FFFF)
    val neonMagenta = Color(0xFFFF00FF)
    val neonGreen = Color(0xFF39FF14)
    val neonYellow = Color(0xFFFFFF00)
    val neonRed = Color(0xFFFF0000)
    val neonBlue = Color(0xFF0000FF)

    // Holographic/UI Palette
    val hologramBlue = Color(0xFF4DFFFF)
    val hologramCyan = Color(0xFF70FFFF)
    val hologramPink = Color(0xFFFF70FF)
    val hologramPurple = Color(0xFFD970FF)
    val hologramGreen = Color(0xFF70FF70)
    val hologramYellow = Color(0xFFFFFF70)
    val hologramRed = Color(0xFFFF7070)

    // Backgrounds & Surfaces
    val neonBackground = Color(0xFF000020) // Deep, dark blue
    val surfaceVariant = Color(0xFF1A1A3A)
    val depthVoid = Color(0xFF000011)
    val depthOcean = Color(0xFF001133)
    val depthMidnight = Color(0xFF050518)
    val depthDark = Color(0xFF0B0B1C)

    // Text Colors
    val textPrimary = Color.White
    val textSecondary = Color(0xFFA0A0E0)
    val textHologram = Color(0xFFD0D0FF)
}
