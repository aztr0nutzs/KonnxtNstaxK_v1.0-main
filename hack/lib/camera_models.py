# Define different camera models and their specific access methods
camera_models = {
    'ModelA': {'url': 'rtsp://{ip}/stream', 'credentials': ['admin', 'password']},
    'ModelB': {'url': 'http://{ip}/video.jpg', 'credentials': ['user', 'pass']},
    # Add more models as needed
}